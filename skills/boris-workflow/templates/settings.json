{
  "_comment": "Example config file. install.sh generates dynamically based on project type and permission level. / 示例配置文件。install.sh 会根据项目类型和权限级别动态生成。",
  "_usage": "Use install.sh --permission-level <minimal|recommended|full> / 使用 install.sh --permission-level 选择权限级别",
  "_see_also": [
    "templates/permissions/minimal.json",
    "templates/permissions/recommended.json",
    "templates/permissions/full.json"
  ],
  "permissions": {
    "allow": [
      "Bash(npm:*)",
      "Bash(npm run:*)",
      "Bash(npx:*)",
      "Bash(git status)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "Read(*)"
    ],
    "deny": [
      "Bash(rm -rf /)",
      "Bash(rm -rf ~)",
      "Bash(sudo:*)",
      "Bash(curl:* | bash)",
      "Bash(git push --force origin main)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "npm run format || true"
          }
        ]
      }
    ]
  }
}
